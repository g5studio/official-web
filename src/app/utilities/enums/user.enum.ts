export enum EIdentity {
    Guest = 'Guest',
    Admin = 'Admin',
    Student = 'Student',
    Enterprise = 'Enterprise'
}

export enum EUserProvider {
    Google = 1,
    FaceBook,
    Twitter,
    Github
}
