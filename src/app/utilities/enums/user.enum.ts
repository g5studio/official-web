export enum EIdentity {
    Guest = 'Guest',
    Admin = 'Admin',
    Student = 'Student',
    Enterprise = 'Enterprise'
}

export enum EUserProvider {
    Google = 1,
    FaceBook,
    Twitter,
    Github
}

export enum EGender {
    Empty = 0,
    Male,
    Female
}

export enum ECategory {
    Frontend = 1,
    Backend,
    Java,
    Unity,
    Python
}
